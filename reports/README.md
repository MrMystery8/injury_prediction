# Reports (generated, not committed)

All pipeline artifacts (CSV/JSON/PNG/HTML) are written under `reports/` and are intentionally gitignored.

Generate them via the runbook in `README.md` or via Streamlit:
- `streamlit run ui/app.py`

