# Data (not committed)

This repo expects local data under `data/`, but **does not commit raw/processed datasets to GitHub**.

Populate `data/raw/` with the Kaggle datasets listed in `README.md`, then run:
- `python3 make_panel.py` to create `data/processed/panel.parquet`

